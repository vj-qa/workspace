package Package;
import Package1.EnumClass;
import  Package1.ObjectClass;
import Package1.EnumClass.months;
public class MainClass {
public static void main(String args[]) {
/*	Obj abc=new Obj();
	abc.show();
	ObjectClass xyz=new ObjectClass();
	xyz.main(args);*/
	
	ReturnType returnobj=new ReturnType();
	returnobj.Add(10,20);
	returnobj.whileCondition();
	//int o= returnobj.Add();
 //System.out.println("the value of addition is  "+returnobj);
 	//EnumClass enumobj=new EnumClass();
	//enumobj.month=EnumClass.months.JAN;
	//int output=0;
	//e.d=E.days.FRIDAY;
	//String output=(String)(EnumClass.months.JAN);

	//System.out.println("Contents of month :"+EnumClass.months.JAN);
	
	//System.out.println(""+enumobj.month.JAN);
	/*int primeArray[]=new int[100];
	boolean done=false;
	
	for (int a=0;!done;  ){
    for(int num=3; ;num++){
	  boolean isPrime=true;  
	  for(int i=2; i<num;i++){
		 
  			if (num%i==0){
  				isPrime=false;
  				  				break;
  			}}
  			if (isPrime) {
  				
  				primeArray[a]=num;
  				a++;
  				if (a==100){done=true;
  			    break;  			    
  				}
  				System.out.println(num+" -This number is prime."+isPrime);
  			}
  			else 
  				System.out.println(num+" This number is  not prime"+isPrime);
  			}
    
    	} 
	for (int j=0;j<100;j++)
		System.out.print(" \n "+primeArray[j]);*/
	}}
  
	